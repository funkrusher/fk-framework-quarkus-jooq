stages:
  - triggers

fk-backend1:
  stage: triggers
  trigger:
    include: .gitlab-ci-fk_backend1.yml
    strategy: depend
  rules:
    - changes: [_services/fk_backend1/**/*, _core/**/*, _modules/fk_product/**/*, _modules/fk_library/**/*, _databases/**/*]

fk-backend2:
  stage: triggers
  trigger:
    include: .gitlab-ci-fk_backend2.yml
    strategy: depend
  rules:
    - changes: [_services/fk_backend2/**/*, _core/**/*, _modules/fk_library/**/*, _databases/**/*]
