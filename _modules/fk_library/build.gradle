plugins {
    id 'java'
    id 'java-library'
    id 'idea'
    id 'io.quarkus'
}

dependencies {
    implementation enforcedPlatform(libs.quarkusPlatform)
    api project(':_databases:fk_database2')

    // decide if you want to resolve fk_core from reposilite, or directly from gradle-module.
    api project(':_core:fk_core')
    // api libs.fkCore

    testImplementation libs.bundles.test.quarkus
    testImplementation libs.restAssured
    testImplementation project(':_databases:fk_database2')
}

// publishing
publishing {
    publications {
        maven(MavenPublication) {
            groupId = theGroup
            artifactId = "fk-library"
            version = theVersion
            from components.java
        }
    }
}
