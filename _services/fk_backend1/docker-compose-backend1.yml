version: "3.5"

services:
  fk-backend1:
    container_name: fk-backend1
    build:
      context: .
      dockerfile: src/main/docker/Dockerfile.jvm
    ports:
      - 9050:8080
    networks:
      - fk-framework

networks:
    fk-framework:
        external: true